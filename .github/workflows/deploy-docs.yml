name: Deploy Documentation

on:
  push:
    branches:
      - main
      # TODO remove after test
      - feature/upgrade-to-mkdocs
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: python-with-uv
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      
      # https://github.com/astral-sh/setup-uv?tab=readme-ov-file#enable-caching
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        
      # More instructions here https://docs.astral.sh/uv/guides/integration/github/#setting-up-python
      - name: Setup Python (using .python-version file)
        run: uv python install
      - name: Install dependencies
        run: |
          uv venv
          uv pip install mkdocs mkdocs-material
      
      - name: Build documentation
        run: uv run mkdocs build --strict
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    # TODO Renenable if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
